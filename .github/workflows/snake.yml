name: generate animation

on:
  # L'animation se met à jour toutes les 24h
  schedule:
    - cron: "0 0 * * *" 
  
  # Permet de lancer l'animation manuellement
  workflow_dispatch:
  
  # Se lance à chaque fois qu'on pousse du code sur la branche main
  push:
    branches:
    - main

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      # Génère le serpent
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      # Pousse le fichier généré sur une branche appelée 'output'
      - name: push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


![Snake animation](https://github.com/serkam31/serkam31/blob/output/github-contribution-grid-snake.svg)